---
phase: 05-hooks-system
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - platform/content-engine/backend-server.js
  - platform/crewai/autonomous_scheduler.py
  - platform/content-engine/hooks-dispatch.test.js
  - platform/crewai/test_hooks_scheduler.py
autonomous: false
requirements:
  - HOOKS-01
  - HOOKS-02
  - HOOKS-03
  - HOOKS-04
  - HOOKS-05

must_haves:
  truths:
    - "Triggered hooks dispatch through the existing `POST /api/agents/:name/run` path with `triggered_by: signal` metadata instead of a parallel execution route"
    - "The backend run path preserves trigger metadata (`trigger_id`, `hook_id`, source signal details) through task creation and run persistence"
    - "The Python APScheduler no longer hard-codes the schedule roster; it reads enabled scheduled hooks from the shared config and fires them at the configured IST cron times"
    - "Signal-driven and scheduled triggers are wired to the same config source, completing the phase goal of one `hooks.json` driving proactive agent runs"
    - "Execution is gated on Phase 4 completing first, because the hook roster and cron ownership are defined against the rewritten 12-agent system"
  artifacts:
    - path: "platform/content-engine/backend-server.js"
      provides: "hook dispatch integration, trigger metadata plumbing, and engine bootstrap/wiring into the existing run endpoint"
      contains: "/api/agents/:name/run"
    - path: "platform/crewai/autonomous_scheduler.py"
      provides: "APScheduler schedule loading from `hooks.json` instead of hard-coded cron definitions"
      contains: "build_scheduler"
    - path: "platform/content-engine/hooks-dispatch.test.js"
      provides: "Automated coverage for run-endpoint trigger metadata and scheduled/signal dispatch plumbing"
    - path: "platform/crewai/test_hooks_scheduler.py"
      provides: "Automated scheduler coverage for hooks.json loading, cron translation, and enabled-hook job registration"
  key_links:
    - from: "platform/content-engine/backend-server.js"
      to: "platform/content-engine/hooks-engine.js"
      via: "dispatchHookRun injection + engine start/stop wiring"
      pattern: "HooksEngine|dispatchHookRun"
    - from: "platform/content-engine/backend-server.js"
      to: "POST /api/agents/:name/run"
      via: "hook dispatch uses the same request body shape plus trigger metadata"
      pattern: "app.post\\(\"/api/agents/:name/run\""
    - from: "platform/crewai/autonomous_scheduler.py"
      to: "platform/content-engine/hooks.json"
      via: "shared scheduled hook config replaces hard-coded CronTrigger literals"
      pattern: "hooks.json|scheduled"
---

<objective>
Complete Phase 5 by wiring both runtime paths to the shared hooks config: hook-triggered signal dispatch through the existing agent run SSE endpoint, and cron-based APScheduler loading from `hooks.json` instead of hard-coded schedules.

Purpose: The hook config and engine are not enough unless the existing run endpoint receives traceable trigger metadata and the scheduler consumes the same source of truth. This plan closes that loop.
Output: `platform/content-engine/backend-server.js`, `platform/crewai/autonomous_scheduler.py`, and `platform/content-engine/hooks-dispatch.test.js`.
Execution precondition: Phase 4 must be complete before this plan runs, because the schedule roster and task semantics assume the rewritten 12-agent framework is already in place.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-hooks-system/05-RESEARCH.md
@.planning/phases/05-hooks-system/05-VALIDATION.md
@platform/content-engine/backend-server.js
@platform/crewai/autonomous_scheduler.py
@platform/content-engine/hooks-config.js
@platform/content-engine/hooks-engine.js
</context>

<interfaces>
Hook-triggered run payload additions for `POST /api/agents/:name/run`:
```json
{
  "company_id": "acme",
  "task": "traffic_drop_recovery_plan",
  "triggered_by": "signal",
  "trigger_id": "<agent_signals.id>",
  "hook_id": "traffic-drop-20pct-7d",
  "trigger_metadata": {
    "signal_type": "traffic_drop_20pct_7d",
    "baseline_value": 100,
    "current_value": 80,
    "delta_pct": 20
  }
}
```

Scheduler responsibilities in this plan:
- load enabled `scheduled` hooks from `platform/content-engine/hooks.json`
- translate five-part cron strings into APScheduler `CronTrigger`
- preserve `Asia/Kolkata` timezone behavior
- dispatch each scheduled hook through the same existing agent execution path used today
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire signal dispatch through the existing run endpoint</name>
  <files>platform/content-engine/backend-server.js</files>
  <behavior>
    - hook-triggered runs reach the same `/api/agents/:name/run` path used for manual invocations
    - request handling accepts and preserves `triggered_by`, `trigger_id`, `hook_id`, and `trigger_metadata`
    - the hook engine can call a shared backend dispatch function instead of duplicating run logic
    - task creation and persisted run output remain compatible with Phase 2 contract behavior
  </behavior>
  <action>
Modify `platform/content-engine/backend-server.js`.

Implementation requirements:
- import and initialize `HooksEngine` using the shared config loader from prior plans
- create a `dispatchHookRun()` helper in backend-server.js that takes the normalized dispatch batch from `HooksEngine`
- for each dispatch item, invoke the same execution path behind `POST /api/agents/:name/run`; do not create a parallel low-level runner
- extend the accepted request body for `/api/agents/:name/run` to include:
  - `triggered_by`
  - `trigger_id`
  - `hook_id`
  - `trigger_metadata`
- ensure those trigger fields are preserved in:
  - prompt/input context passed to the run
  - saved contract input metadata or other persisted run metadata already used by the backend
  - any follow-up `agent_tasks` rows created from the run where appropriate
- start the engine on backend startup and stop it cleanly on shutdown if the file already has graceful process handlers

Keep existing SSE behavior intact. Manual calls to `/api/agents/:name/run` must continue to work unchanged when the new trigger fields are absent.
  </action>
  <verify>
    <automated>rg -n "triggered_by|trigger_id|hook_id|trigger_metadata|HooksEngine|dispatchHookRun" /Users/yogs87/Documents/New\ project/marqq/platform/content-engine/backend-server.js</automated>
  </verify>
  <done>
Signal-triggered runs are dispatched through the existing backend run path with explicit trigger metadata and without bypassing validation, MKG patching, or task persistence.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace hard-coded scheduler entries with hooks.json-driven schedule loading</name>
  <files>platform/crewai/autonomous_scheduler.py</files>
  <behavior>
    - APScheduler reads enabled scheduled hooks from the shared config file
    - CronTrigger objects are created from config rather than embedded literals
    - scheduler retains IST timezone behavior and heartbeat updates
    - scheduled agent dispatch remains traceable back to the hook id/config entry
  </behavior>
  <action>
Modify `platform/crewai/autonomous_scheduler.py`.

Implementation requirements:
- load `platform/content-engine/hooks.json` at scheduler startup
- filter to enabled `scheduled` entries
- translate each five-part cron expression into APScheduler `CronTrigger` arguments
- replace the current hard-coded `scheduler.add_job(... CronTrigger(...))` list in `build_scheduler()` with a loop over config entries
- preserve existing heartbeat/status updates and logging
- pass schedule metadata such as `hook_id` or `task_type` through the existing scheduler execution path so scheduled runs are attributable to the config entry that fired

Do not remove APScheduler or change the scheduler’s timezone away from `Asia/Kolkata`.
  </action>
  <verify>
    <automated>python3 -m py_compile /Users/yogs87/Documents/New\ project/marqq/platform/crewai/autonomous_scheduler.py</automated>
  </verify>
  <done>
Scheduled hooks are now driven by the shared config file, completing the single-source-of-truth requirement for cron-based agent execution.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests for trigger metadata and dispatch wiring</name>
  <files>platform/content-engine/hooks-dispatch.test.js</files>
  <action>
Create `platform/content-engine/hooks-dispatch.test.js` using the Node built-in test runner.

Cover:
- `dispatchHookRun()` sends `triggered_by: "signal"` plus `trigger_id`, `hook_id`, and `trigger_metadata`
- `/api/agents/:name/run` continues to accept manual calls without trigger metadata
- triggered runs preserve trigger metadata in the saved input/persistence layer shape used by the backend
- dispatch order matches the configured hook `dispatch[].order`

Keep these tests local and dependency-injected. Do not rely on live Supabase, real Groq calls, or wall-clock scheduler waits.
  </action>
  <verify>
    <automated>node --test /Users/yogs87/Documents/New\ project/marqq/platform/content-engine/hooks-dispatch.test.js</automated>
  </verify>
  <done>
Automated coverage exists for HOOKS-05 metadata plumbing, and the remaining live checks are limited to the manual heartbeat and wall-clock validations already called out in Phase 5 validation.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add scheduler-focused automated coverage for hooks.json schedule loading</name>
  <files>platform/crewai/test_hooks_scheduler.py</files>
  <action>
Create `platform/crewai/test_hooks_scheduler.py` using Python's built-in `unittest`.

Cover:
- loading enabled scheduled hooks from `platform/content-engine/hooks.json`
- translating five-part cron expressions into APScheduler-compatible fields
- preserving configured `Asia/Kolkata` timezone behavior
- registering the expected enabled jobs without hard-coded schedule literals

Keep the test local and dependency-light. Stub scheduler registration if needed; do not wait on real cron boundaries.
  </action>
  <verify>
    <automated>python3 -m unittest /Users/yogs87/Documents/New\ project/marqq/platform/crewai/test_hooks_scheduler.py</automated>
  </verify>
  <done>
Automated scheduler coverage exists for the scheduled hook path, so HOOKS-01 is no longer validated only by `py_compile` and manual wall-clock observation.
  </done>
</task>

</tasks>

<verification_criteria>
- Hook-driven dispatch uses `POST /api/agents/:name/run` semantics with `triggered_by: "signal"` metadata preserved end to end.
- The scheduler reads enabled cron hooks from `platform/content-engine/hooks.json` instead of hard-coded jobs.
- `node --test platform/content-engine/hooks-dispatch.test.js` passes locally.
- `python3 -m unittest platform.crewai.test_hooks_scheduler` passes locally.
- Manual validation remains for real Supabase-backed heartbeat timing and real cron boundary execution, matching `05-VALIDATION.md`.
</verification_criteria>
