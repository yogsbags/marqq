---
phase: 05-hooks-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/content-engine/hooks.json
  - platform/content-engine/hooks-config.js
  - platform/content-engine/hooks-config.test.js
autonomous: true
requirements:
  - HOOKS-01
  - HOOKS-02
  - HOOKS-03
  - HOOKS-04
  - HOOKS-05

must_haves:
  truths:
    - "`platform/content-engine/hooks.json` is the single source of truth for scheduled and signal-triggered runs, with `scheduled`, `signal_triggers`, and `chat_triggers` sections present"
    - "Config validation rejects malformed hook ids, unknown agents, malformed cron strings, duplicate ids, and missing required signal mappings before runtime dispatch starts"
    - "The config loader exposes timezone and heartbeat settings so both Node signal evaluation and Python scheduler wiring consume the same hook definitions"
    - "The required signal mappings exist in config for `traffic_drop_20pct_7d`, `new_company_onboarded`, `campaign_anomaly`, and `competitor_move`, aligned to the rewritten agent roster"
  artifacts:
    - path: "platform/content-engine/hooks.json"
      provides: "Shared hook definitions for scheduled, signal, and reserved chat trigger sections"
      contains: "signal_triggers"
    - path: "platform/content-engine/hooks-config.js"
      provides: "loadHooksConfig(), validateHooksConfig(), parseCronExpression(), and runtime-safe accessors for scheduled/signal hooks"
      exports: ["loadHooksConfig", "validateHooksConfig", "parseCronExpression"]
    - path: "platform/content-engine/hooks-config.test.js"
      provides: "Wave 0 coverage for config shape, required mappings, duplicate id rejection, agent validation, and cron validation"
  key_links:
    - from: "platform/content-engine/hooks-config.js"
      to: "platform/content-engine/hooks.json"
      via: "readFile + JSON.parse + validateHooksConfig"
      pattern: "loadHooksConfig"
    - from: "platform/content-engine/hooks-config.js"
      to: "platform/content-engine/backend-server.js"
      via: "shared VALID_AGENTS-compatible roster validation for hook dispatch safety"
      pattern: "VALID_AGENTS|allowedAgents"
---

<objective>
Create the Phase 5 hook configuration layer: the checked-in `hooks.json` file plus the loader/validator that every later plan depends on.

Purpose: Phase 5 cannot safely add signal polling or scheduled dispatch until the hook definitions, required mappings, cron syntax, and agent roster are centralized and validated at startup instead of being hard-coded ad hoc in separate runtimes.
Output: `platform/content-engine/hooks.json`, `platform/content-engine/hooks-config.js`, and `platform/content-engine/hooks-config.test.js`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hooks-system/05-RESEARCH.md
@.planning/phases/05-hooks-system/05-VALIDATION.md
@platform/content-engine/backend-server.js
@platform/crewai/autonomous_scheduler.py
</context>

<interfaces>
hooks.json must follow this top-level shape:
```json
{
  "version": 1,
  "timezone": "Asia/Kolkata",
  "heartbeat_seconds": 60,
  "scheduled": [],
  "signal_triggers": [],
  "chat_triggers": []
}
```

Signal trigger minimums required by roadmap/research:
- `traffic_drop_20pct_7d` => tara, kiran
- `new_company_onboarded` => veena, isha, neel, zara
- `campaign_anomaly` => zara, dev
- `competitor_move` => priya, neel

Config loader responsibilities:
- validate unique ids across all hook sections
- validate required top-level sections exist
- validate every referenced agent name is in the allowed runtime roster
- validate cron strings well enough for APScheduler translation later
- expose scheduled hooks and signal hooks in normalized form for Plans 05-02 and 05-03
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hooks.json with the full Phase 5 trigger inventory</name>
  <files>platform/content-engine/hooks.json</files>
  <behavior>
    - config contains `scheduled`, `signal_triggers`, and `chat_triggers`
    - scheduled section contains at least 6 enabled hook definitions aligned to roadmap intent, including Veena weekly refresh
    - signal section contains at least 7 enabled definitions, including the 4 required mappings from HOOKS-02
    - every hook definition has a stable `id`, `enabled`, and explicit dispatch metadata
    - signal rules declare a diff operator and threshold target instead of raw absolute firing only
  </behavior>
  <action>
Create `platform/content-engine/hooks.json` as the shared config file for Phase 5.

Populate:
- `version: 1`
- `timezone: "Asia/Kolkata"`
- `heartbeat_seconds: 60`
- `scheduled`: minimum 6 enabled entries
- `signal_triggers`: minimum 7 enabled entries
- `chat_triggers`: empty array for now, but must exist because the roadmap slice explicitly names it

Required scheduled examples to include:
- `veena-weekly-refresh` => agent `veena`, cron `0 6 * * mon`
- additional hooks for the rewritten roster such as daily/weekly monitors for `isha`, `neel`, `tara`, `zara`, `dev`

Required signal mappings to include:
- `traffic_drop_20pct_7d` => sequential dispatch to `tara` then `kiran`
- `new_company_onboarded` => sequential dispatch chain `veena`, `isha`, `neel`, `zara`
- `campaign_anomaly` => `zara`, `dev`
- `competitor_move` => `priya`, `neel`

For each signal trigger, include:
- `id`
- `signal_type`
- `enabled`
- `dispatch` array with `agent`, `task_type`, and `order`
- `condition` object with `metric_path`, `baseline_path`, `operator`, and `threshold`
- `cooldown_minutes`

Add at least three more realistic signal definitions so the file reaches the roadmap minimum of 7 signal triggers.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='/Users/yogs87/Documents/New project/marqq/platform/content-engine/hooks.json'; const data=JSON.parse(fs.readFileSync(p,'utf8')); if(!Array.isArray(data.scheduled)||data.scheduled.length<6) throw new Error('scheduled<6'); if(!Array.isArray(data.signal_triggers)||data.signal_triggers.length<7) throw new Error('signal_triggers<7'); if(!Array.isArray(data.chat_triggers)) throw new Error('missing chat_triggers'); const types=new Set(data.signal_triggers.map(x=>x.signal_type)); ['traffic_drop_20pct_7d','new_company_onboarded','campaign_anomaly','competitor_move'].forEach(t=>{ if(!types.has(t)) throw new Error('missing '+t); }); console.log('hooks.json shape OK');"</automated>
  </verify>
  <done>
`hooks.json` exists, includes all required top-level sections, meets the minimum scheduled and signal counts, and contains the HOOKS-02 trigger mappings that later plans will execute.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the config loader and validator</name>
  <files>platform/content-engine/hooks-config.js</files>
  <behavior>
    - `loadHooksConfig()` reads and validates hooks.json once and returns normalized config
    - `validateHooksConfig()` throws actionable errors for duplicates, missing required sections, missing HOOKS-02 mappings, unknown agents, or malformed dispatch blocks
    - cron validation is strict enough to reject malformed expressions before scheduler startup
    - validation enforces `heartbeat_seconds` as a positive integer and timezone presence
  </behavior>
  <action>
Create `platform/content-engine/hooks-config.js` as an ESM module.

Export:
- `loadHooksConfig()`
- `validateHooksConfig(config)`
- `parseCronExpression(cronText)`

Implementation requirements:
- read `hooks.json` from the same directory using `node:fs/promises`
- define one canonical allowed-agent roster in this file using the rewritten runtime names from requirements/research
- validate unique ids across `scheduled`, `signal_triggers`, and `chat_triggers`
- validate required top-level keys exist
- validate every `dispatch[].agent` is in the allowed roster
- validate HOOKS-02 mappings are present exactly once by `signal_type`
- validate each signal trigger has `condition.metric_path`, `condition.baseline_path`, `condition.operator`, and numeric `condition.threshold`
- validate cron strings are 5-part cron expressions that Plan 05-03 can translate into APScheduler fields
- normalize omitted optional fields like `cooldown_minutes` to sensible defaults

Do not wire the scheduler or engine here. This file is purely config loading and validation.
  </action>
  <verify>
    <automated>node --check /Users/yogs87/Documents/New\ project/marqq/platform/content-engine/hooks-config.js && node -e \"import('/Users/yogs87/Documents/New project/marqq/platform/content-engine/hooks-config.js').then(async m => { const cfg = await m.loadHooksConfig(); console.log(cfg.timezone, cfg.heartbeat_seconds); if (!cfg.signal_triggers?.length) throw new Error('no signal triggers'); }).catch(err => { console.error(err); process.exit(1); })\"</automated>
  </verify>
  <done>
The config loader is syntactically valid, exports the required functions, and fails fast when the checked-in hook definitions are malformed or incomplete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 tests for config correctness</name>
  <files>platform/content-engine/hooks-config.test.js</files>
  <action>
Create `platform/content-engine/hooks-config.test.js` using the Node built-in test runner.

Cover:
- valid checked-in config loads successfully
- duplicate hook ids are rejected
- unknown agent names are rejected
- missing HOOKS-02 signal mappings are rejected
- malformed cron strings are rejected
- missing `scheduled`, `signal_triggers`, or `chat_triggers` sections are rejected

Keep the tests deterministic and file-local. Do not require Supabase, scheduler startup, or real network access.
  </action>
  <verify>
    <automated>node --test /Users/yogs87/Documents/New\ project/marqq/platform/content-engine/hooks-config.test.js</automated>
  </verify>
  <done>
Wave 0 test coverage exists for the config layer and the validation strategy file can mark the config references as present for Phase 5.
  </done>
</task>

</tasks>

<verification_criteria>
- `platform/content-engine/hooks.json` exists and is the only checked-in Phase 5 hook config file.
- The config includes the required HOOKS-02 signal mappings and enough scheduled/signal entries to satisfy the roadmap minimums.
- `loadHooksConfig()` and `validateHooksConfig()` reject malformed config before runtime dispatch begins.
- `node --test platform/content-engine/hooks-config.test.js` passes locally.
</verification_criteria>
