---
phase: 07-swarm-layer
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - database/migrations/swarm-competitive-intelligence.sql
  - platform/content-engine/swarm/spreadsheet-swarm.ts
  - platform/content-engine/swarm/competitive-intelligence-repo.ts
  - platform/content-engine/swarm/agent-signals-persistence.ts
  - platform/content-engine/swarm/spreadsheet-swarm.test.ts
  - platform/content-engine/swarm/competitive-intelligence.test.ts
autonomous: true
requirements:
  - SWARM-03
  - SWARM-04
  - SWARM-05

must_haves:
  truths:
    - "SpreadsheetSwarm runs Haiku for filtering up to 300 transcripts and 400 tweets, then passes only the filtered items to Sonnet synthesis so token spend stays bounded."
    - "`competitive_intelligence` table exists with composite key `(company_id, competitor_name, week_of)` and stores summaries, themes, actions, `haiku_count`, `sonnet_tokens`, and `source_run_id`."
    - "Signals emitted after synthesis become Supabase `agent_signals` rows (`competitor_move`, `competitor_content_pub`) that include `source_run_id` plus the `agent` that emitted them so HooksEngine can chain follow-ups."
    - "Tests cover the Haiku/Sonnet pipeline with deterministic sample batches so the plan has verification before live data touches connectors."
  artifacts:
    - path: "database/migrations/swarm-competitive-intelligence.sql"
      provides: "Supabase migration for the `competitive_intelligence` table plus supporting indexes and RLS policies."
      contains: "competitive_intelligence"
    - path: "platform/content-engine/swarm/spreadsheet-swarm.ts"
      provides: "SpreadsheetSwarm ingestion surface that splits filtering (Haiku) and synthesis (Sonnet) phases and records `filtered_count` before handing off to Sonnet."
      contains: "filterCorpus"
    - path: "platform/content-engine/swarm/competitive-intelligence-repo.ts"
      provides: "Persistence helpers that upsert `competitive_intelligence` entries and read the latest weekly row per competitor for validation."
      contains: "upsertCompetitiveIntelligence"
    - path: "platform/content-engine/swarm/agent-signals-persistence.ts"
      provides: "Utility to write `agent_signals` rows after synthesis, tagging the signal with `run_id`, `competitor_name`, and `signal_payload` that HooksEngine can read."
      contains: "emitSignal"
  key_links:
    - from: "platform/content-engine/swarm/competitive-intelligence-repo.ts"
      to: "database/migrations/swarm-competitive-intelligence.sql"
      via: "Repo upserts into the table defined in the migration, aligning fields and RLS expectations."
      pattern: "upsert"
    - from: "platform/content-engine/swarm/spreadsheet-swarm.ts"
      to: "platform/content-engine/swarm/agent-signals-persistence.ts"
      via: "After Sonnet produces insights, SpreadsheetSwarm calls the signal emitter so HooksEngine gets `competitor_move` records in the same `run_id`."
      pattern: "emitSignal"

<objective>
Implement the SpreadsheetSwarm corpus handling, `competitive_intelligence` persistence, and signal emission (SWARM-03 → SWARM-05). This plan includes the migration, APIs, telemetry exposures, and unit tests that guarantee Haiku filtering feeds Sonnet synthesis and the resulting insights are stored + signaled before Hooks evaluation.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-swarm-layer/07-RESEARCH.md
@CLAUDE.md

@platform/content-engine/mkg-service.js
@platform/content-engine/hooks-engine.js
</context>

<interfaces>
SpreadsheetSwarm must be configurable for two phases: `filterCorpus(batch)` (Haiku) which returns filtered items and `synthesizeInsights(filteredItems)` (Sonnet) which returns structured `summary`, `themes`, `actions`, `confidence`, and token counts. Signal persistence needs to write to `agent_signals` with meta fields `signal_type`, `source_run_id`, `competitor_name`, `signal_payload`, and `emitted_at`. The `competitive_intelligence` table enforces unique `(company_id, competitor_name, week_of)` rows to prevent duplicates and stores `haiku_count` and `sonnet_tokens` for telemetry.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Ship SpreadsheetSwarm filtering + Sonnet synthesis pipeline and tests</name>
  <files>
    platform/content-engine/swarm/spreadsheet-swarm.ts
    platform/content-engine/swarm/spreadsheet-swarm.test.ts
  </files>
  <action>
Implement SpreadsheetSwarm: `filterCorpus` takes up to 300 transcripts + 400 tweets, runs Haiku filtering (cheap model stub), drops low-scoring items, and returns filtered items plus metadata. `synthesizeInsights` runs Sonnet on the filtered payload to produce structured insight fields and token usage counts. Build deterministic tests covering: (a) Haiku dropping irrelevant items, (b) Sonnet receiving only filtered items, and (c) telemetry output (`haiku_count`, `filtered_count`, `sonnet_tokens`) matches the research requirements.
  </action>
  <verify>
    <automated>node --test platform/content-engine/swarm/spreadsheet-swarm.test.ts</automated>
    <manual>Feed the pipeline a recorded corpus and confirm the Sonnet stub only sees the filtered subset, then verify telemetry metadata matches the counts.</manual>
  </verify>
  <done>
SpreadsheetSwarm handles corpus filtering and synthesis with deterministic tests so later plans rely on stable behavior when connecting to real connectors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Persist `competitive_intelligence` and emit signals</name>
  <files>
    database/migrations/swarm-competitive-intelligence.sql
    platform/content-engine/swarm/competitive-intelligence-repo.ts
    platform/content-engine/swarm/agent-signals-persistence.ts
    platform/content-engine/swarm/competitive-intelligence.test.ts
  </files>
  <action>
Create the Supabase migration for `competitive_intelligence` with PK columns, uniqueness on `(company_id, competitor_name, week_of)`, timestamps, and `agent_signals` RW metrics (haiku/sonnet). Implement the repo to upsert rows including `source_run_id`, `summary`, `themes`, `actions`, `confidence`, `haiku_count`, and `sonnet_tokens`. Add a persistence helper that writes `agent_signals` rows with `signal_type` `competitor_move`/`competitor_content_pub`. Cover the persistence logic with tests that assert the repo calls the migration's table and the signal emitter adds entries with the right metadata.
  </action>
  <verify>
    <automated>node --test platform/content-engine/swarm/competitive-intelligence.test.ts</automated>
    <manual>Apply `database/migrations/swarm-competitive-intelligence.sql` in Supabase, run a Priya swarm, and confirm the `competitive_intelligence` row plus `agent_signals` entries exist for the same `run_id` and that HooksEngine would see them within 60 seconds.</manual>
  </verify>
  <done>
Insights persist to Supabase and signals emit after synthesis so HooksEngine can trigger RiYa immediately, meeting SWARM-04 and SWARM-05.
  </done>
</task>

</tasks>

## Verification Criteria

- `node --test platform/content-engine/swarm/spreadsheet-swarm.test.ts` covers Haiku and Sonnet stages before executing real swings.
- `node --test platform/content-engine/swarm/competitive-intelligence.test.ts` exercises the repository upserts and signal emission helpers.
- `database/migrations/swarm-competitive-intelligence.sql` can be applied manually; the table enforces `(company_id, competitor_name, week_of)` uniqueness and includes `haiku_count`/`sonnet_tokens`.
- Manual run writes both a `competitive_intelligence` row and one or more `agent_signals` `competitor_move` entries sharing the same `run_id`, ready for HooksEngine to dispatch follow-ons.
