---
phase: 07-swarm-layer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/content-engine/swarm/swarm-runner.ts
  - platform/content-engine/swarm/run-scheduler.ts
  - platform/content-engine/swarm/swarm-watchdog-helper.ts
  - platform/content-engine/swarm/swarm-watchdog-telemetry.ts
  - platform/content-engine/swarm/swarm-watchdog.test.ts
autonomous: true
requirements:
  - SWARM-01

must_haves:
  truths:
    - "Priya's swarm-runner orchestrates a ConcurrentWorkflow: fan-out to 10 Watchdog subtasks, await them, and fan-in once every watcher completes so the synthesis node is always after all delta work."
    - "Delta enforcement happens before any expensive work—each Watchdog compares connector `published_after`/`last_checked` and skips Sonnet/Haiku unless new content exists."
    - "`swarm_watchdog_runs` telemetry records every watcher run (run_id, competitor, delta window, tokens, status) so downstream verification can sample watchers as outlined in research."
    - "A scheduler helper can rehydrate jobs (cron or Hooks) and tracks `next_run_at`, keeping the retries/concurrency caps aligned with Phase 7's target throughput."
  artifacts:
    - path: "platform/content-engine/swarm/swarm-runner.ts"
      provides: "ConcurrentWorkflow entrypoint that fans out to watchers, enforces delta-only fetches, and fans in to synthesis."
      contains: "fanOutWatchdogs"
    - path: "platform/content-engine/swarm/run-scheduler.ts"
      provides: "Scheduler utility that records `swarm_watchdog_runs`, enforces `maxConcurrency`, and surfaces the next `jobWindow` for SpreadsheetSwarm."
      contains: "scheduleSwarmRun"
    - path: "platform/content-engine/swarm/swarm-watchdog-helper.ts"
      provides: "Shared helpers for extracting `last_checked`, calling connectors, and triggering `swarm_watchdog_runs` inserts before signaling the runner."
      contains: "buildWatchdogDelta"
    - path: "platform/content-engine/swarm/swarm-watchdog-telemetry.ts"
      provides: "Telemetry helpers that log watcher durations, token usage, and `delta_start/end` to `swarm_watchdog_runs` for auditing."
      contains: "logWatchdogRun"
  key_links:
    - from: "platform/content-engine/swarm/swarm-runner.ts"
      to: "platform/content-engine/mkg-service.js"
      via: "reads `mkg.competitors` to seed Watchdog inputs and write back updated `last_checked` values"
      pattern: "mkg.competitors"
    - from: "platform/content-engine/swarm/run-scheduler.ts"
      to: "platform/content-engine/hooks-engine.js"
      via: "scheduler exposes the same `jobWindow` concept that HooksEngine uses for signal-triggered executions"
      pattern: "jobWindow"

<objective>
Wire up the swarm runner runtime that satisfies SWARM-01: the concurrent execution surface for Priya's Watchdogs, delta enforcement, and the telemetry foundation that feeds the validation architecture described in research. The objective is to deliver a reusable scheduler helper, fan-out/fan-in control, and `swarm_watchdog_runs` logging before any higher-order behavior lands.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-swarm-layer/07-RESEARCH.md
@CLAUDE.md

@platform/content-engine/mkg-service.js
@platform/content-engine/hooks-engine.js
</context>

<interfaces>
The ConcurrentWorkflow fan-out must rehydrate from `mkg.competitors[].last_checked` and skip connectors when no delta exists; Watchdogs that discover zero new content must return without invoking Haiku/Sonnet so the delta-only rule is enforced. The scheduler helper must expose a `maxConcurrency` guard and `jobWindow` metadata so future plans and Hooks can sample the same execution boundary. Every watcher run writes a row in `swarm_watchdog_runs` before returning so plan 07-03 can inspect telemetry in tests and SQL checks.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the swarm runtime and scheduler helpers</name>
  <files>
    platform/content-engine/swarm/swarm-runner.ts
    platform/content-engine/swarm/run-scheduler.ts
    platform/content-engine/swarm/swarm-watchdog-helper.ts
    platform/content-engine/swarm/swarm-watchdog-telemetry.ts
    platform/content-engine/swarm/swarm-watchdog.test.ts
  </files>
  <action>
Draft `swarm-runner.ts` that loads MKG competitors, fans out to 10 `watchdog` helper invocations, and fans back in for synthesis once all watchers resolve. Create `run-scheduler.ts` to persist job windows, enforce concurrency caps, and record the next `next_run_at`. Add helpers to gather `last_checked`, call stub connectors, and insert `swarm_watchdog_runs` telemetry before returning. Cover the runtime surface with a test that asserts:
- concurrent watcher calls happen (Promise.all semantics),
- delta enforcement short-circuits when no new connector data exists,
- and `swarm_watchdog_runs` entries are emitted with the expected fields.
  </action>
  <verify>
    <automated>node --test platform/content-engine/swarm/swarm-watchdog.test.ts</automated>
    <manual>Inspect the emitted `swarm_watchdog_runs` rows in Supabase to ensure every watcher run persists `delta_start`, `delta_end`, and `status` before synthesis begins.</manual>
  </verify>
  <done>
The swarm runtime exists with concurrency controls, delta enforcement, and validated telemetry; downstream plans reuse the scheduler helper and log entries to inspect signal timing.
  </done>
</task>

</tasks>

## Verification Criteria

- `platform/content-engine/swarm/swarm-runner.ts` fans out to exactly 10 Watchdogs and waits for every completion before synthesis.
- `swarm_watchdog_runs` logs exist for every watcher call, including `delta_start/end`, `tokens_used`, and `status`.
- `node --test platform/content-engine/swarm/swarm-watchdog.test.ts` confirms concurrency/delta behaviors before the plan is marked complete.
- The scheduler helper exports `scheduleSwarmRun` plus `jobWindow` metadata so HooksEngine can align with the same timing.
