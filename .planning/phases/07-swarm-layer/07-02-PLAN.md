---
phase: 07-swarm-layer
plan: "02"
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - platform/content-engine/swarm/priya-swarm-config.ts
  - platform/content-engine/swarm/mkg-watchdog-connectors.ts
  - platform/content-engine/swarm/priya-swarm-config.test.ts
  - platform/content-engine/hooks/priya-hook-responder.ts
autonomous: true
requirements:
  - SWARM-02

must_haves:
  truths:
    - "Priya's swarm configuration instantiates 10 Watchdog definitions (one per competitor) and wires each to `mkg.competitors[n]` metadata, ensuring every competitor has preferred connectors and `last_checked` defaults."
    - "Every Watchdog updates `mkg.competitors[n].last_checked` and `watchdog_history` immediately after its delta window resolves so future runs use the correct anchor."
    - "Stub connector hooks per channel (YouTube, Twitter, RSS, press release) exist for fast testing; real connectors can be swapped in later without touching the config shape."
    - "`priya-hook-responder.ts` translates `agent_signals` such as `competitor_move` into HookEngine-friendly payloads and ensures the `HooksEngine` metadata (`source_agent`, `signal_payload`, `run_id`) is present."
  artifacts:
    - path: "platform/content-engine/swarm/priya-swarm-config.ts"
      provides: "Central configuration enumerating 10 competitor Watchdogs, their connector stubs, and MKG update logic."
      contains: "competitorWatchdogs"
    - path: "platform/content-engine/swarm/mkg-watchdog-connectors.ts"
      provides: "Connector abstraction that binds MKG `preferred_connectors` entries to actual fetch helpers (YouTube/Twitter/RSS/press)."
      contains: "connectorsByType"
    - path: "platform/content-engine/hooks/priya-hook-responder.ts"
      provides: "Hook responder that persists `agent_signals` for `competitor_move` events and surfaces them to HooksEngine."
      contains: "emitCompetitorSignal"
    - path: "platform/content-engine/swarm/priya-swarm-config.test.ts"
      provides: "Unit coverage ensuring config exports 10 Watchdogs, the `last_checked` update path functions, and stub connectors obey delta filtering."
      contains: "describe('Priya Swarm Config')"
  key_links:
    - from: "platform/content-engine/swarm/priya-swarm-config.ts"
      to: "platform/content-engine/mkg-service.js"
      via: "Each Watchdog reads and writes competitor-specific fields such as `last_checked` and `baseline_content`."
      pattern: "competitors"
    - from: "platform/content-engine/hooks/priya-hook-responder.ts"
      to: "platform/content-engine/hooks-engine.js"
      via: "Hook responder formats `agent_signals` rows so HooksEngine can identify `competitor_move` signals within 60 seconds."
      pattern: "agent_signals"

<objective>
Define Priya's swarm manifest: 10 competitor Watchdogs, connector stubs, MKG updater helpers, and a Hook responder so downstream signal processing sees `competitor_move` in `agent_signals`. This plan locks the config shape and ensures `last_checked` deltas are enforced before the Sql/SpreadsheetSwarm persistence work begins.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-swarm-layer/07-RESEARCH.md
@CLAUDE.md

@platform/content-engine/mkg-service.js
@platform/content-engine/hooks-engine.js
</context>

<interfaces>
Priya's config must expose `competitorWatchdogs` as an array of 10 objects, each with `competitorName`, `connectors`, and `lastCheckedPath`. Connectors must accept `(companyId, competitorName, since)` and return placeholder batches when real connectors are missing. The Hook responder writes `agent_signals` rows with `signal_type` `competitor_move` plus `signal_payload` referencing the same `run_id` that generated the `competitive_intelligence` row, letting HooksEngine chain Priya→Riya runs within 60 seconds.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Ship Priya's 10-competitor configuration and Hook responder</name>
  <files>
    platform/content-engine/swarm/priya-swarm-config.ts
    platform/content-engine/swarm/mkg-watchdog-connectors.ts
    platform/content-engine/hooks/priya-hook-responder.ts
    platform/content-engine/swarm/priya-swarm-config.test.ts
  </files>
  <action>
Create a config module that enumerates 10 competitors, resolver helpers for MKG `preferred_connectors`, and `last_checked` update logic. Provide connector stubs (YouTube/Twitter/RSS/press) so tests can assert `since`/`last_checked` filtering. Build `priya-hook-responder.ts` that inserts `agent_signals` rows tagging the signal with `competitor_name` and `source_run_id`. Exercise the config via tests ensuring the exported `competitorWatchdogs` length is 10, `last_checked` updates propagate, and Hook responder emits structured signal metadata.
  </action>
  <verify>
    <automated>node --test platform/content-engine/swarm/priya-swarm-config.test.ts</automated>
    <manual>Run a dry Priya swarm against sample MKG data, confirm `mkg.competitors[n].last_checked` updates, and inspect `agent_signals` for `competitor_move` rows containing the new `run_id`.</manual>
  </verify>
  <done>
Priya's configuration and Hook responder exist with delta-aware connectors, `last_checked` updates, and signal emission, fulfilling SWARM-02 ready for the next plan's persistence work.
  </done>
</task>

</tasks>

## Verification Criteria

- Config exports exactly 10 competitor Watchdogs with stubbed connectors and `last_checked` logic.
- `priya-hook-responder.ts` writes `agent_signals` entries for `competitor_move` with `source_run_id`/`competitor_name`.
- Automated test `node --test platform/content-engine/swarm/priya-swarm-config.test.ts` passes before the plan is marked complete.
- Manual dry run updates MKG `last_checked` and surfaces signal rows for HooksEngine to consume within the same cycle.
