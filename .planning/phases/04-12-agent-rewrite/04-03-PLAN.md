---
phase: 04-12-agent-rewrite
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - platform/content-engine/backend-server.js
  - platform/crewai/autonomous_scheduler.py
  - test-agent-registry.js
  - test-agent-run-contract.js
autonomous: true
requirements:
  - AGENT-07
  - AGENT-08
  - AGENT-01
  - AGENT-06

must_haves:
  truths:
    - "Backend `VALID_AGENTS` includes veena plus all 11 rewritten agents, including kiran and sam."
    - "Backend `AGENT_PROFILES` reflect the Phase 4 role map rather than the pre-rewrite role descriptions."
    - "Scheduler role map, crew routing, cron jobs, and prompt assembly cover the intended 12-agent roster with IST schedules."
    - "An explicit per-agent schedule matrix defines which agents are daily, weekly, or event-driven."
    - "Dedicated smoke verification hits the real `POST /api/agents/:name/run` endpoint for all 12 agents and checks valid AgentRunOutput plus non-empty `context_patch`."
  artifacts:
    - path: "platform/content-engine/backend-server.js"
      provides: "Authoritative run-endpoint roster and profile metadata"
      contains: "VALID_AGENTS"
    - path: "platform/crewai/autonomous_scheduler.py"
      provides: "IST cron scheduling and scheduler-facing role/crew map"
      contains: "build_scheduler"
    - path: "platform/crewai/agents/schedule-matrix.json"
      provides: "Per-agent cadence contract tied to Phase-4 roles"
    - path: "test-agent-registry.js"
      provides: "Static assertions for agent filesystem, backend roster, scheduler roster, and connector validity"
    - path: "test-agent-run-contract.js"
      provides: "Smoke test for valid AgentRunOutput and non-empty context_patch across the agent roster"
  key_links:
    - from: "test-agent-run-contract.js"
      to: "platform/content-engine/contract-validator.js"
      via: "reuse validateContract() to assert contract correctness"
      pattern: "validateContract"
    - from: "platform/crewai/autonomous_scheduler.py"
      to: "platform/crewai/agents/*"
      via: "scheduler load_soul/load_memory read filesystem agent directories"
      pattern: "AGENTS_DIR / agent_name"
---

<objective>
Align the runtime with the rewritten 12-agent filesystem by updating backend registration, scheduler coverage, and adding explicit smoke verification for the roadmap success criteria.

Purpose: A successful rewrite is not just documentation on disk. The new roster has to be runnable through `POST /api/agents/:name/run`, schedulable in IST, and guarded by tests that catch registry drift and empty context patches.
Output: Updated backend/scheduler registration plus two smoke-test scripts for registry and contract validation.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-12-agent-rewrite/04-RESEARCH.md
@.planning/phases/04-12-agent-rewrite/04-VALIDATION.md
@.planning/phases/04-12-agent-rewrite/04-01-PLAN.md
@.planning/phases/04-12-agent-rewrite/04-02-PLAN.md

@platform/content-engine/backend-server.js
@platform/content-engine/contract-validator.js
@platform/content-engine/mcp-router.js
@platform/crewai/autonomous_scheduler.py
</context>

<interfaces>
Backend run registration currently gates agent execution here:

```javascript
if (!VALID_AGENTS.has(name)) {
  return res.status(404).json({ error: "Unknown agent" });
}
```

Scheduler runtime depends on these structures:

```python
AGENT_ROLES = {...}
AGENT_CREWS = {...}
scheduler.add_job(...)
```

Contract verification should reuse the existing validator:

```javascript
import { validateContract } from "./platform/content-engine/contract-validator.js";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Align backend roster and agent profile metadata with the rewritten 12-agent map</name>
  <files>platform/content-engine/backend-server.js</files>
  <action>
Update `backend-server.js` so the backend runtime reflects the Phase 4 roster:

- `VALID_AGENTS` must include `veena` plus all 11 rewritten agents
- add missing `kiran` and `sam`
- keep `isha`, `neel`, and `tara`
- refresh `AGENT_PROFILES` titles, personality blurbs, and execution bullets so they match the locked role map

Do not leave stale descriptions such as `dev` = paid media or `zara` = orchestrator if those conflict with AGENT-05.
  </action>
  <verify>
    <automated>node --check platform/content-engine/backend-server.js && node test-agent-registry.js --check-backend-roster</automated>
  </verify>
  <done>
The backend run endpoint recognizes the full rewritten roster and exposes profile metadata consistent with Phase 4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align scheduler role map, prompt assembly, crew routing, and IST job schedule</name>
  <files>
    platform/crewai/autonomous_scheduler.py
    platform/crewai/agents/schedule-matrix.json
  </files>
  <action>
Rewrite the scheduler execution path so it reflects the rewritten 12-agent system and does not bypass the MKG-first foundation defined in Phase 4.

Requirements:
- cover `veena` plus all 11 rewritten agents
- preserve `Asia/Kolkata` timezone
- assign explicit cadences that match the roadmap phrase “weekly briefs, daily monitors”
- remove legacy scheduling assumptions tied to the old 8-agent model
- ensure scheduled runs consume the same agent foundation as interactive runs by loading `skills/*.md` with `00-product-marketing-context.md` first, or by calling a shared prompt builder that guarantees equivalent behavior

Create `platform/crewai/agents/schedule-matrix.json` with one entry per rewritten agent containing:
- `agent`
- `phase4_role`
- `cadence_type` (`daily_monitor`, `weekly_brief`, `event_driven`)
- `cron_ist` (or `null` for event-driven)
- short rationale tied to the role

Then make `autonomous_scheduler.py` implement the matrix rather than hardcoding disconnected cron choices.

If some agents are better event-driven than daily, encode that explicitly in comments or schedule choices, but keep the file internally consistent.
  </action>
  <verify>
    <automated>python3 -m py_compile platform/crewai/autonomous_scheduler.py && node test-agent-registry.js --check-scheduler --check-scheduler-prompt-path --check-schedule-matrix</automated>
  </verify>
  <done>
Scheduler metadata, prompt assembly, schedule matrix, and jobs are aligned to the rewritten 12-agent roster and IST cadence.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add registry and contract smoke tests for the rewritten roster</name>
  <files>
    test-agent-registry.js
    test-agent-run-contract.js
  </files>
  <action>
Create two root-level test scripts:

1. `test-agent-registry.js`
   - inspect `platform/crewai/agents/`
   - assert all 12 expected agent names exist
   - assert SOUL/memory/mcp/skills presence per phase rules
   - assert `00-product-marketing-context.md` exists and is first by sort order
   - assert every connector declared in `mcp.json` is known to `CONNECTOR_APP_MAP`
   - assert backend `VALID_AGENTS` includes the same roster
   - assert scheduler source includes the same roster
   - assert scheduler prompt assembly reads skills or otherwise proves the `00-product-marketing-context.md` foundation is present for scheduled runs
   - assert `platform/crewai/agents/schedule-matrix.json` exists and covers all rewritten agents

2. `test-agent-run-contract.js`
   - reuse `extractContract()` and `validateContract()`
   - boot the backend locally in a test-safe mode and hit the real `POST /api/agents/:name/run` HTTP endpoint for each of the 12 agents
   - assert returned contract is valid
   - assert `context_patch.patch` is non-empty

Prefer a test-safe mode that avoids live external connectors but still exercises the real HTTP run path. Do not replace this with a pure simulation; the roadmap success criterion explicitly requires confirmation via `POST /api/agents/:name/run`.
  </action>
  <verify>
    <automated>node test-agent-registry.js && node test-agent-run-contract.js</automated>
  </verify>
  <done>
The phase has executable smoke verification for roster alignment, explicit schedule contract, and non-empty AgentRunOutput context patches via the real run endpoint.
  </done>
</task>

</tasks>
